package shapes;


// measurable is the actions. (the verbs)
public interface Measurable {

//        return (2 * this.length) + (2 * this.width);
    double getPerimeter();

//        return this.side * this.side;
    double getArea();

}